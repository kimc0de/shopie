import {StyleSheet} from 'react-native';
import {Space} from '../../styles';

export const styles = StyleSheet.create({
  category: {
    marginBottom: Space.SPACE_1,
    minHeight: Space.SPACE_4,
  },
});
